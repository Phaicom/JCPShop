export class Market {
  id: string; // fireStore id use to delete and update
  name: string;
  email: string;
  phoneNumber: number;
  photoURL: string;
  description: string;
  date: string; // opening date
  mid: string;

  constructor(parameters) {

  }
}
